import * as stories from './Image.stories'
import { Meta, Primary, Canvas } from "@storybook/addon-docs/blocks";

<Meta of={stories} />

# Definition

Une image.

<Primary />

Quand une erreur survient (source invalide, image non-disponible...), l'image affichera un *placeholder*.

<Canvas of={stories.sourceInvalide} />
